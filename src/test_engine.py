from kata_alpaca_engine import KataAlpacaEngine

#TODO(Move this later into testing)
def test_engine(): 
    """
    tests creation of stream files and directories 
    """

    kata_engine = KataAlpacaEngine(archive_mode=True)

test_engine()